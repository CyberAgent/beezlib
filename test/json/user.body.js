/**
 * @name user.body.js
 */
(function () {
    var body = {
        head: 'head1',
        body: 'body1',
    };


    module.exports = body;

    console.log('user.body.loaded');
    return body;
}());
